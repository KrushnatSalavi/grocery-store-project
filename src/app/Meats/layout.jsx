import React from 'react';


export const metadata = {
    title: "MEATS PAGE",
    description : "Meats Page",
};

const MeatsPageLayout = ({children}) => {
  return (
    <>
      {children}

    </>
  )
}

export default MeatsPageLayout

